# Rapport M0-2 — Lancer catalog-update.yml une fois — fraîcheur non nulle

Date : 2026-09-12 · Taille : S · Dépend de : M0-1 · Débloque : M2-9.

## Ce qui a été fait

Trois runs manuels de `catalog-update.yml` pour arriver à une PR fusionnable :

- **#1 (échec)** : les vérificateurs ont tourné (Kiwix 228 changements, GitHub 435, modèles 108,
  registres 45, liens 17) mais `validate` a refusé 8 ressources : les vérificateurs posaient
  `notes.en` seul, le schéma exige `fr` + `en` → `setUpdaterNote()` (PR #1).
- **#2 (vert, PR #2 ouverte)** : mais 25 ZIM passés à `missing` (Wikipédia, Wiktionnaire,
  Appropedia, LibreTexts, iFixit…). Cause : Kiwix publie désormais `<name>wikipedia_fr_all</name>`
  + `<flavour>maxi</flavour>` là où le catalogue dit `wikipedia_fr_all_maxi` → résolution nom +
  saveur, jamais `missing` sur erreur réseau, `phet_fr` → `phet_fr_all`, artefacts uploadés
  (`include-hidden-files`) (PR #3). Aucun artefact n'avait été uploadé (dossier caché).
- **#3 (vert)** : PR #2 rafraîchie sur la même branche, 0 faux `missing`, artefact
  `catalog-reports` présent. Relue (tests 178/178, catalogue valide, champs sensibles parcourus,
  7 `missing` restants justifiés un par un) puis fusionnée par Florian (`5e2059a`).

## Critères d'acceptation

- [x] Le workflow s'exécute sans erreur et ouvre une PR (pas d'automerge — étiquettes `automated`,
  `catalog`, `catalog-sensitive` posées par `catalog-review`)
- [x] `arche catalog freshness` : **93 %** — 294/315 ressources vérifiées dans leur intervalle
  (0 périmées, 21 jamais vérifiées ; seuil du ticket : 50 %, seuil du workflow : 70 %)

## Ce que le premier passage a révélé (→ M2-9)

Trois faux négatifs de type de suivi (mosquitto, git-vesc, ngspice), trois liens morts réels
(ema-herbal-monographs, plantnet-offline, git-openbionics), huit motifs d'assets GitHub à réécrire,
une fausse alerte de taille (devdocs-en), et 42 liens en 403 dont la plupart sont des pare-feux
anti-robot. `ros2-docs` reste `missing` tant que M0-3 n'a pas uploadé l'item Internet Archive.
